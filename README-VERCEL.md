# Vercel 部署指南

## 🚀 快速部署步骤

### 1. 准备代码

确保代码已上传到 GitHub：

```bash
# 在代码目录中执行
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/您的用户名/ai-email-platform.git
git push -u origin main
```

### 2. Vercel 部署

1. 访问 https://vercel.com
2. 点击 **Add New Project**
3. 导入 GitHub 仓库 `ai-email-platform`
4. 配置如下：
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
5. 点击 **Deploy**

### 3. 配置环境变量（可选）

在 Vercel 项目设置中，添加环境变量：

```
GEMINI_API_KEY = 您的Gemini API密钥（如需真实AI功能）
```

获取 API Key: https://makersuite.google.com/app/apikey

---

## 📋 功能说明

### 当前版本（Mock 模式）

| 功能 | 状态 | 说明 |
|------|------|------|
| 首页展示 | ✅ | 完整页面 |
| 注册/登录 | ✅ | 验证码直接显示 |
| 定价页面 | ✅ | 完整展示 |
| 控制台 | ✅ | 可查看 |
| 生成开发信 | ✅ | 模拟生成（非真实AI） |
| 邮件历史 | ✅ | 本地存储 |

### 演示模式特点

- **验证码可见**：点击"获取验证码"后，验证码会显示在页面上
- **模拟AI生成**：生成的是预设模板邮件，非真实AI生成
- **数据不持久**：刷新页面后数据会丢失

---

## 🔧 启用真实AI功能

如需使用真实的 Gemini AI 功能，需要：

1. 获取 Gemini API Key
2. 配置后端服务器（Node.js + Express）
3. 部署到支持 Node.js 的平台（Railway, Render, 云服务器等）

详细后端部署文档见 `README.md`
